# Project 2 — submission_model inference dependencies.
# Tested with Python 3.11, torch 2.6.0+cu124, torchvision 0.21.0 (CPU works too).
# Pinning is loose on purpose; the versions below are known-good lower bounds.
torch>=2.1
torchvision>=0.16
opencv-python>=4.8
numpy>=1.24,<2.0
scikit-learn>=1.3
Pillow>=9.0
